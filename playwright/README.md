# Playwright tests

## Prerequisites
- Node.js 18+

## Install
```bash
cd playwright
npm ci
npx playwright install --with-deps
```

## Run
```bash
npm test
```

## Notes
- Base URL is configured as `https://www.epam.com` in `playwright.config.ts`.
